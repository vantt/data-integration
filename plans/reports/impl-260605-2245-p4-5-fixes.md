# P4-5 Fixes Implementation Report

**Date:** 2026-06-05 | **Agent:** fullstack-developer

---

## FIX 1 — detailView promo double-count (DONE)

**File:** `detailView/app/adapters/inbound/web/templates/partials/order/_financial.html`

**Change:**
- Removed standalone `−Promo goods cost` waterfall deduction row (was `waterfall_row("−", "Promo goods cost ...", neg=True)` inside a `{% if f.promo_goods_cost > 0 %}` block).
- Replaced with `<tr class="wf-row wf-row--annotation">` block containing a muted `caption`-styled note: `"trong đó hàng tặng (promo): {{ f.promo_goods_cost | vnd }}"` — conditional on `promo_goods_cost > 0`, placed immediately after the COGS row. No amount in running total.
- Updated header comment block to explain promo is an attribution label (already in cogs_amount), not a deduction.

**Verification (curl 2603035YC1UJNR):**
- HTTP 200. Channel net profit = `+213.112 ₫` (correct; matches `net_revenue − cogs_amount + shopee_fees`).
- Waterfall rows: `wf-row--annotation` present (informational promo note `60.185 ₫`), no `−Promo goods cost` deduction row.
- No UndefinedError.

**No-promo order (SON03232):** HTTP 200, no annotation row rendered (conditional block skipped correctly).

---

## FIX 2 — dbt guard tests (DONE)

**Files created:**

`transformation/tests/assert_promo_account_not_in_keep_pool.sql`
- Asserts no account with `treatment = 'drop_promo_count_once'` appears with any `treatment LIKE 'keep_%'` in `stg_overhead_account_classification`.
- Returns offending `(account, treatment, pool_id)` rows on violation.

`transformation/tests/assert_no_promo_in_overhead_costs.sql`
- Asserts no rows in `fact_order_costs` where `cost_category = 'OVERHEAD'` AND `cost_type LIKE '%promo%'`.
- Returns offending `(order_id, order_code, cost_type, cost_category, amount)` rows on violation.

**dbt test results (all 3):**
```
1 of 3 PASS assert_no_promo_in_overhead_costs   [PASS in 0.04s]
2 of 3 PASS assert_overhead_allocation_closure  [PASS in 0.04s]
3 of 3 PASS assert_promo_account_not_in_keep_pool [PASS in 0.04s]

PASS=3 WARN=0 ERROR=0 SKIP=0 TOTAL=3
```

---

## FIX 3 — int_promo_642_monthly_total docstring (DONE)

**File:** `transformation/models/intermediate/cogs/int_promo_642_monthly_total.sql`

**Change (comment-only):** Replaced false claim "Phase-04 subtracts this amount from the overhead expense pool" with accurate description:
- Exclusion mechanism = classification treatment (`drop_promo_count_once`), not subtraction.
- Model is MONITORING/AUDIT ONLY — not referenced by any downstream mart.
- Noted the standalone XK promo gap (~40M/year 2026) as accepted business limitation.
- Tags and SQL unchanged.

---

## Steps Executed

| Step | Result |
|------|--------|
| 3 edits applied | Done |
| `docker compose restart data_platform` | Done, Up healthy |
| `dbt test` (3 tests) | PASS=3 |
| `docker compose up -d --build detail_view` | Built + healthy |
| curl 2603035YC1UJNR | HTTP 200, promo shown as annotation, no double-count deduction |
| curl SON03232 | HTTP 200, no annotation row |

---

## Files Modified

| File | Type |
|------|------|
| `detailView/app/adapters/inbound/web/templates/partials/order/_financial.html` | Template edit |
| `transformation/tests/assert_promo_account_not_in_keep_pool.sql` | New |
| `transformation/tests/assert_no_promo_in_overhead_costs.sql` | New |
| `transformation/models/intermediate/cogs/int_promo_642_monthly_total.sql` | Comment edit |
