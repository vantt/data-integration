══════════════════════════════════════════════════════════════════════
METABASE DASHBOARD DEBUGGER
URL: http://127.0.0.1:3001/dashboard/42-daily-sales-retail?ordered_at=thisday&tab=123-t%E1%BB%95ng-quan
══════════════════════════════════════════════════════════════════════
[cache] Dashboard 42 loaded from cache (age: 17m). Use --no-cache to refresh.
Dashboard : "Daily Sales [Retail]" (ID: 42)
Mô tả     : Finalized review of yesterday's **retail** sales performance — Health Score gauge, KPIs with integrated DoD trends, hourly patterns, channel/product/customer breakdowns across 4 tabs.

Dashboard parameters (1):
  ✓ [date/all-options] "ordered_at" = "thisday"
  ⚠️  slug="tab" không có trong dashboard parameters

Cards: 35 data cards | 15 text/heading

──────────────────────────────────────────────────────────────────────
Card 1/35: "Health Score"
card_id=1217 | dashcard_id=1710 | tab="Tổng quan"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  WITH
  recent AS (
      SELECT
          COALESCE(SUM(o.net_revenue), 0) as revenue,
          COUNT(DISTINCT o.order_id) as orders,
          CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
               ELSE SUM(o.net_revenue) / COUNT(DISTINCT o.order_id) END as aov
      FROM fact_orders o
      JOIN dim_customers c ON o.customer_key = c.customer_key
      WHERE date(o.ordered_at) BETWEEN current_date - INTERVAL '7 days' AND current_date - INTERVAL '1 day'
        AND o.scope_retail
        AND o.is_active_order
  ),
  previous AS (
      SELECT
          COALESCE(SUM(o.net_revenue), 0) as revenue,
          COUNT(DISTINCT o.order_id) as orders,
          CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
               ELSE SUM(o.net_revenue) / COUNT(DISTINCT o.order_id) END as aov
      FROM fact_orders o
    ... (+27 more lines)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Health Score
  Health Score
  90

──────────────────────────────────────────────────────────────────────
Card 2/35: "Health Breakdown"
card_id=1218 | dashcard_id=1711 | tab="Tổng quan"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  WITH
  recent AS (
      SELECT
          COALESCE(SUM(o.net_revenue), 0) as revenue,
          COUNT(DISTINCT o.order_id) as orders,
          CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
               ELSE SUM(o.net_revenue) / COUNT(DISTINCT o.order_id) END as aov
      FROM fact_orders o
      JOIN dim_customers c ON o.customer_key = c.customer_key
      WHERE date(o.ordered_at) BETWEEN current_date - INTERVAL '7 days' AND current_date - INTERVAL '1 day'
        AND o.scope_retail
        AND o.is_active_order
  ),
  previous AS (
      SELECT
          COALESCE(SUM(o.net_revenue), 0) as revenue,
          COUNT(DISTINCT o.order_id) as orders,
          CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
               ELSE SUM(o.net_revenue) / COUNT(DISTINCT o.order_id) END as aov
      FROM fact_orders o
    ... (+46 more lines)

[KẾT QUẢ]
  Tổng rows : 4
  Columns   : sort, Thành phần, Thay đổi %, Điểm, Status
  sort | Thành phần | Thay đổi % | Điểm | Status
  1 | Doanh thu (WoW) | 601.6 | 25 | OK
  2 | Đơn hàng (WoW) | 118.8 | 25 | OK
  3 | Khách quay lại | 43.8 | 20 | OK
  4 | AOV ổn định | 220.7 | 20 | OK

──────────────────────────────────────────────────────────────────────
Card 3/35: "Net Revenue"
card_id=1219 | dashcard_id=1712 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COALESCE(SUM(o.net_revenue), 0) as "Net Revenue"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT COALESCE(SUM(o.net_revenue), 0) as "Net Revenue"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Net Revenue
  Net Revenue
  2384007

──────────────────────────────────────────────────────────────────────
Card 4/35: "Gross Revenue"
card_id=1220 | dashcard_id=1713 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COALESCE(SUM(o.gross_revenue), 0) as "Gross Revenue"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT COALESCE(SUM(o.gross_revenue), 0) as "Gross Revenue"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Gross Revenue
  Gross Revenue
  4625300

──────────────────────────────────────────────────────────────────────
Card 5/35: "Total Orders"
card_id=1221 | dashcard_id=1714 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COUNT(DISTINCT o.order_id) as "Total Orders"
  FROM fact_orders o
  WHERE {{ordered_at}}
    AND o.scope_retail

[SQL THỰC TẾ (sau substitute)]
  SELECT COUNT(DISTINCT o.order_id) as "Total Orders"
  FROM fact_orders o
  WHERE o.scope_retail
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Total Orders
  Total Orders
  6

──────────────────────────────────────────────────────────────────────
Card 6/35: "AOV"
card_id=1222 | dashcard_id=1715 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
           ELSE ROUND(SUM(o.net_revenue) / COUNT(DISTINCT o.order_id), 0) END as "AOV"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT
      CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
           ELSE ROUND(SUM(o.net_revenue) / COUNT(DISTINCT o.order_id), 0) END as "AOV"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : AOV
  AOV
  596002

──────────────────────────────────────────────────────────────────────
Card 7/35: "New Customers"
card_id=1223 | dashcard_id=1716 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COUNT(DISTINCT o.customer_key) as "New Customers"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND date(c.first_order_date) = date(o.ordered_at)
    AND o.scope_retail

[SQL THỰC TẾ (sau substitute)]
  SELECT COUNT(DISTINCT o.customer_key) as "New Customers"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE date(c.first_order_date) = date(o.ordered_at)
    AND o.scope_retail
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : New Customers
  New Customers
  2

──────────────────────────────────────────────────────────────────────
Card 8/35: "Returning Customers"
card_id=1224 | dashcard_id=1717 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COUNT(DISTINCT o.customer_key) as "Returning Customers"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND date(c.first_order_date) < date(o.ordered_at)
    AND o.scope_retail

[SQL THỰC TẾ (sau substitute)]
  SELECT COUNT(DISTINCT o.customer_key) as "Returning Customers"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE date(c.first_order_date) < date(o.ordered_at)
    AND o.scope_retail
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Returning Customers
  Returning Customers
  2

──────────────────────────────────────────────────────────────────────
Card 9/35: "Returns"
card_id=1225 | dashcard_id=1718 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COUNT(DISTINCT o.order_id) as "Returns"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.fulfillment_status = 'RETURNED'
    AND o.scope_retail

[SQL THỰC TẾ (sau substitute)]
  SELECT COUNT(DISTINCT o.order_id) as "Returns"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.fulfillment_status = 'RETURNED'
    AND o.scope_retail
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Returns
  Returns
  0

──────────────────────────────────────────────────────────────────────
Card 10/35: "Total Collected"
card_id=1226 | dashcard_id=1719 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COALESCE(SUM(o.total_collected), 0) as "Total Collected"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT COALESCE(SUM(o.total_collected), 0) as "Total Collected"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Total Collected
  Total Collected
  2572800

──────────────────────────────────────────────────────────────────────
Card 11/35: "Discount Rate"
card_id=1227 | dashcard_id=1720 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      ROUND(
          COUNT(DISTINCT CASE WHEN o.discount_amount > 0 THEN o.order_id END) * 100.0
          / NULLIF(COUNT(DISTINCT o.order_id), 0), 1
      ) as "Discount Rate %"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT
      ROUND(
          COUNT(DISTINCT CASE WHEN o.discount_amount > 0 THEN o.order_id END) * 100.0
          / NULLIF(COUNT(DISTINCT o.order_id), 0), 1
      ) as "Discount Rate %"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Discount Rate %
  Discount Rate %
  75

──────────────────────────────────────────────────────────────────────
Card 12/35: "Items per Order"
card_id=1228 | dashcard_id=1721 | tab="Tổng quan"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  SELECT ROUND(
      SUM(s.quantity)::FLOAT / NULLIF(COUNT(DISTINCT s.order_id), 0), 1
  ) as "Items/Order"
  FROM fact_sales s
  JOIN fact_orders o ON s.order_id = o.order_id
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE date(s.ordered_at) = current_date - INTERVAL '1 day'
    AND o.scope_retail

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Items/Order
  Items/Order
  1.4

──────────────────────────────────────────────────────────────────────
Card 13/35: "Hourly Sales Trend"
card_id=1229 | dashcard_id=1722 | tab="Tổng quan"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  WITH yesterday_sales AS (
      SELECT
          EXTRACT(HOUR FROM o.ordered_at) as hour_of_day,
          SUM(o.net_revenue) as sales_yesterday
      FROM fact_orders o
      JOIN dim_customers c ON o.customer_key = c.customer_key
      WHERE date(o.ordered_at) = current_date - INTERVAL '1 day'
        AND o.scope_retail
        AND o.is_active_order
      GROUP BY 1
  ),
  day_before_sales AS (
      SELECT
          EXTRACT(HOUR FROM o.ordered_at) as hour_of_day,
          SUM(o.net_revenue) as sales_day_before
      FROM fact_orders o
      JOIN dim_customers c ON o.customer_key = c.customer_key
      WHERE date(o.ordered_at) = current_date - INTERVAL '2 days'
        AND o.scope_retail
        AND o.is_active_order
    ... (+9 more lines)

[KẾT QUẢ]
  Tổng rows : 8
  Columns   : Hour, Hôm qua, Hôm kia
  Hour | Hôm qua | Hôm kia
  0 | 0 | 3072332
  9 | 0 | 97319
  12 | 0 | 2375000
  17 | 0 | 2250000
  18 | 1610000 | 768519

──────────────────────────────────────────────────────────────────────
Card 14/35: "Cumulative Revenue"
card_id=1230 | dashcard_id=1723 | tab="Tổng quan"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  WITH hours AS (
      SELECT UNNEST(GENERATE_SERIES(0, 23)) as hour_of_day
  ),
  yesterday_hourly AS (
      SELECT
          EXTRACT(HOUR FROM o.ordered_at) as hour_of_day,
          SUM(o.net_revenue) as revenue
      FROM fact_orders o
      JOIN dim_customers c ON o.customer_key = c.customer_key
      WHERE date(o.ordered_at) = current_date - INTERVAL '1 day'
        AND o.scope_retail
        AND o.is_active_order
      GROUP BY 1
  ),
  day_before_hourly AS (
      SELECT
          EXTRACT(HOUR FROM o.ordered_at) as hour_of_day,
          SUM(o.net_revenue) as revenue
      FROM fact_orders o
      JOIN dim_customers c ON o.customer_key = c.customer_key
    ... (+13 more lines)

[KẾT QUẢ]
  Tổng rows : 24
  Columns   : Hour, Hôm qua (Lũy kế), Hôm kia (Lũy kế)
  Hour | Hôm qua (Lũy kế) | Hôm kia (Lũy kế)
  0 | 0 | 3072332
  1 | 0 | 3072332
  2 | 0 | 3072332
  3 | 0 | 3072332
  4 | 0 | 3072332

──────────────────────────────────────────────────────────────────────
Card 15/35: "Revenue by Channel"
card_id=1231 | dashcard_id=1724 | tab="Kênh bán hàng"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      ch.channel_name as "Kênh",
      SUM(o.net_revenue) as "Doanh thu"
  FROM fact_orders o
  JOIN dim_channels ch ON o.channel_key = ch.channel_key
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order
  GROUP BY 1
  ORDER BY 2 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      ch.channel_name as "Kênh",
      SUM(o.net_revenue) as "Doanh thu"
  FROM fact_orders o
  JOIN dim_channels ch ON o.channel_key = ch.channel_key
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1
  ORDER BY 2 DESC

[KẾT QUẢ]
  Tổng rows : 4
  Columns   : Kênh, Doanh thu
  Kênh | Doanh thu
  Web | 1475952
  Shopee - JPC SHOP | 816296
  Shopee - Fine Japan Vietnam | 91759
  Đại Lý | 0

──────────────────────────────────────────────────────────────────────
Card 16/35: "Revenue by Channel Category"
card_id=1232 | dashcard_id=1725 | tab="Kênh bán hàng"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      ch.channel_category as "Loại kênh",
      SUM(o.net_revenue) as "Doanh thu",
      COUNT(DISTINCT o.order_id) as "Đơn hàng"
  FROM fact_orders o
  JOIN dim_channels ch ON o.channel_key = ch.channel_key
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order
  GROUP BY 1
  ORDER BY 2 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      ch.channel_category as "Loại kênh",
      SUM(o.net_revenue) as "Doanh thu",
      COUNT(DISTINCT o.order_id) as "Đơn hàng"
  FROM fact_orders o
  JOIN dim_channels ch ON o.channel_key = ch.channel_key
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1
  ORDER BY 2 DESC

[KẾT QUẢ]
  Tổng rows : 2
  Columns   : Loại kênh, Doanh thu, Đơn hàng
  Loại kênh | Doanh thu | Đơn hàng
  Online-Ecommerce | 2384007 | 3
  Offline | 0 | 1

──────────────────────────────────────────────────────────────────────
Card 17/35: "Channel Performance vs Day Before"
card_id=1233 | dashcard_id=1726 | tab="Kênh bán hàng"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  WITH yesterday AS (
      SELECT
          ch.channel_name,
          COUNT(DISTINCT o.order_id) as orders,
          COALESCE(SUM(o.net_revenue), 0) as revenue
      FROM fact_orders o
      JOIN dim_channels ch ON o.channel_key = ch.channel_key
      JOIN dim_customers c ON o.customer_key = c.customer_key
      WHERE date(o.ordered_at) = current_date - INTERVAL '1 day'
        AND o.scope_retail
        AND o.is_active_order
      GROUP BY 1
  ),
  day_before AS (
      SELECT
          ch.channel_name,
          COUNT(DISTINCT o.order_id) as orders,
          COALESCE(SUM(o.net_revenue), 0) as revenue
      FROM fact_orders o
      JOIN dim_channels ch ON o.channel_key = ch.channel_key
    ... (+17 more lines)

[KẾT QUẢ]
  Tổng rows : 3
  Columns   : Kênh, Đơn hôm qua, Đơn hôm kia, DT hôm qua, DT hôm kia, Thay đổi %
  Kênh | Đơn hôm qua | Đơn hôm kia | DT hôm qua | DT hôm kia | Thay đổi %
  Shopee - JPC SHOP | 2 | 3 | 3324931 | 3156765 | 5.3
  Shopee - Fine Japan Vietnam | 0 | 6 | 0 | 7162947 | -100
  Shopee - thehealthyus | 0 | 2 | 0 | 276069 | -100

──────────────────────────────────────────────────────────────────────
Card 18/35: "Sales by Branch"
card_id=1234 | dashcard_id=1727 | tab="Kênh bán hàng"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      bl.branch_location_name as "Chi nhánh",
      COUNT(DISTINCT o.order_id) as "Đơn hàng",
      COALESCE(SUM(o.net_revenue), 0) as "Doanh thu",
      CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
           ELSE ROUND(SUM(o.net_revenue) / COUNT(DISTINCT o.order_id), 0) END as "AOV"
  FROM fact_orders o
  JOIN dim_branch_location bl ON o.branch_location_key = bl.branch_location_key
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order
  GROUP BY 1
  ORDER BY 3 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      bl.branch_location_name as "Chi nhánh",
      COUNT(DISTINCT o.order_id) as "Đơn hàng",
      COALESCE(SUM(o.net_revenue), 0) as "Doanh thu",
      CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
           ELSE ROUND(SUM(o.net_revenue) / COUNT(DISTINCT o.order_id), 0) END as "AOV"
  FROM fact_orders o
  JOIN dim_branch_location bl ON o.branch_location_key = bl.branch_location_key
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1
  ORDER BY 3 DESC

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Chi nhánh, Đơn hàng, Doanh thu, AOV
  Chi nhánh | Đơn hàng | Doanh thu | AOV
  16 Trương Định | 4 | 2384007 | 596002

──────────────────────────────────────────────────────────────────────
Card 19/35: "Top 10 Products by Revenue"
card_id=1235 | dashcard_id=1728 | tab="Sản phẩm"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      p.product_name as "Sản phẩm",
      SUM(s.net_revenue) as "Doanh thu"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE {{ordered_at}} AND scope_retail AND is_active_order
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1
  ORDER BY 2 DESC
  LIMIT 10

[SQL THỰC TẾ (sau substitute)]
  SELECT
      p.product_name as "Sản phẩm",
      SUM(s.net_revenue) as "Doanh thu"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail AND is_active_order
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1
  ORDER BY 2 DESC
  LIMIT 10

[KẾT QUẢ]
  Tổng rows : 4
  Columns   : Sản phẩm, Doanh thu
  Sản phẩm | Doanh thu
  Thực phẩm bảo vệ sức khỏe Natto Kinase | 2288424.671962552
  Viên uống trắng da Coix Beauty tablets With Vitamin C- 200 viên | 176943.94450050453
  Công Văn Giấy Tờ | 0
  Thực phẩm bảo vệ sức khỏe Hyaluron & Collagen Plus | 0

──────────────────────────────────────────────────────────────────────
Card 20/35: "Top 10 Products by Quantity"
card_id=1236 | dashcard_id=1729 | tab="Sản phẩm"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      p.product_name as "Sản phẩm",
      SUM(s.quantity) as "Số lượng"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE {{ordered_at}} AND scope_retail
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1
  ORDER BY 2 DESC
  LIMIT 10

[SQL THỰC TẾ (sau substitute)]
  SELECT
      p.product_name as "Sản phẩm",
      SUM(s.quantity) as "Số lượng"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1
  ORDER BY 2 DESC
  LIMIT 10

[KẾT QUẢ]
  Tổng rows : 4
  Columns   : Sản phẩm, Số lượng
  Sản phẩm | Số lượng
  Thực phẩm bảo vệ sức khỏe Natto Kinase | 4
  Viên uống trắng da Coix Beauty tablets With Vitamin C- 200 viên | 4
  Thực phẩm bảo vệ sức khỏe Hyaluron & Collagen Plus | 2
  Công Văn Giấy Tờ | 1

──────────────────────────────────────────────────────────────────────
Card 21/35: "Revenue by Product Type"
card_id=1237 | dashcard_id=1730 | tab="Sản phẩm"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      COALESCE(p.product_type, 'Unknown') as "Loại SP",
      SUM(s.net_revenue) as "Doanh thu"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE {{ordered_at}} AND scope_retail AND is_active_order
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1
  ORDER BY 2 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      COALESCE(p.product_type, 'Unknown') as "Loại SP",
      SUM(s.net_revenue) as "Doanh thu"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail AND is_active_order
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1
  ORDER BY 2 DESC

[KẾT QUẢ]
  Tổng rows : 2
  Columns   : Loại SP, Doanh thu
  Loại SP | Doanh thu
  lots_date | 2465368.6164630563
  normal | 0

──────────────────────────────────────────────────────────────────────
Card 22/35: "Product Performance Table"
card_id=1238 | dashcard_id=1731 | tab="Sản phẩm"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      p.product_name as "Sản phẩm",
      COALESCE(p.product_type, 'Unknown') as "Loại",
      SUM(s.quantity) as "SL",
      SUM(s.net_revenue) as "Doanh thu",
      ROUND(SUM(s.net_revenue) / NULLIF(SUM(s.quantity), 0), 0) as "Giá TB"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE {{ordered_at}} AND scope_retail AND is_active_order
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1, 2
  ORDER BY 4 DESC
  LIMIT 20

[SQL THỰC TẾ (sau substitute)]
  SELECT
      p.product_name as "Sản phẩm",
      COALESCE(p.product_type, 'Unknown') as "Loại",
      SUM(s.quantity) as "SL",
      SUM(s.net_revenue) as "Doanh thu",
      ROUND(SUM(s.net_revenue) / NULLIF(SUM(s.quantity), 0), 0) as "Giá TB"
  FROM fact_sales s
  JOIN (
      SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail AND is_active_order
  ) o ON s.order_id = o.order_id
  JOIN dim_products p ON s.product_key = p.product_key
  GROUP BY 1, 2
  ORDER BY 4 DESC
  LIMIT 20

[KẾT QUẢ]
  Tổng rows : 4
  Columns   : Sản phẩm, Loại, SL, Doanh thu, Giá TB
  Sản phẩm | Loại | SL | Doanh thu | Giá TB
  Thực phẩm bảo vệ sức khỏe Natto Kinase | lots_date | 2 | 2288424.671962552 | 1144212
  Viên uống trắng da Coix Beauty tablets With Vitamin C- 200 viên | lots_date | 2 | 176943.94450050453 | 88472
  Thực phẩm bảo vệ sức khỏe Hyaluron & Collagen Plus | lots_date | 2 | 0 | 0
  Công Văn Giấy Tờ | normal | 1 | 0 | 0

──────────────────────────────────────────────────────────────────────
Card 23/35: "Returning Customer Rate"
card_id=1239 | dashcard_id=1732 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      ROUND(
          COUNT(DISTINCT CASE WHEN date(c.first_order_date) < date(o.ordered_at) THEN o.customer_key END) * 100.0
          / NULLIF(COUNT(DISTINCT o.customer_key), 0), 1
      ) as "Returning Rate %"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail

[SQL THỰC TẾ (sau substitute)]
  SELECT
      ROUND(
          COUNT(DISTINCT CASE WHEN date(c.first_order_date) < date(o.ordered_at) THEN o.customer_key END) * 100.0
          / NULLIF(COUNT(DISTINCT o.customer_key), 0), 1
      ) as "Returning Rate %"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Returning Rate %
  Returning Rate %
  50

──────────────────────────────────────────────────────────────────────
Card 24/35: "At Risk Customers"
card_id=1240 | dashcard_id=1733 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS] không có — card này không bị ảnh hưởng bởi dashboard filters

[SQL TEMPLATE]
  -- dim_customers has no scope column; filter directly on customer_type dimension
  SELECT COUNT(*) as "At Risk Customers"
  FROM dim_customers
  WHERE customer_status = 'At Risk'
    AND customer_type IN ('RETAIL')

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : At Risk Customers
  At Risk Customers
  178

──────────────────────────────────────────────────────────────────────
Card 25/35: "New vs Returning Customers"
card_id=1241 | dashcard_id=1734 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      CASE
          WHEN date(c.first_order_date) = date(o.ordered_at) THEN 'Khách mới'
          ELSE 'Khách quay lại'
      END as "Loại KH",
      COUNT(DISTINCT o.order_id) as "Đơn hàng",
      SUM(o.net_revenue) as "Doanh thu"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order
  GROUP BY 1

[SQL THỰC TẾ (sau substitute)]
  SELECT
      CASE
          WHEN date(c.first_order_date) = date(o.ordered_at) THEN 'Khách mới'
          ELSE 'Khách quay lại'
      END as "Loại KH",
      COUNT(DISTINCT o.order_id) as "Đơn hàng",
      SUM(o.net_revenue) as "Doanh thu"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1

[KẾT QUẢ]
  Tổng rows : 2
  Columns   : Loại KH, Đơn hàng, Doanh thu
  Loại KH | Đơn hàng | Doanh thu
  Khách mới | 2 | 1475952
  Khách quay lại | 2 | 908055

──────────────────────────────────────────────────────────────────────
Card 26/35: "Revenue by Customer Segment"
card_id=1242 | dashcard_id=1735 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      COALESCE(c.value_group, 'Unknown') as "Phân khúc",
      COUNT(DISTINCT o.order_id) as "Đơn hàng",
      SUM(o.net_revenue) as "Doanh thu",
      CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
           ELSE ROUND(SUM(o.net_revenue) / COUNT(DISTINCT o.order_id), 0) END as "AOV"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order
  GROUP BY 1
  ORDER BY 3 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      COALESCE(c.value_group, 'Unknown') as "Phân khúc",
      COUNT(DISTINCT o.order_id) as "Đơn hàng",
      SUM(o.net_revenue) as "Doanh thu",
      CASE WHEN COUNT(DISTINCT o.order_id) = 0 THEN 0
           ELSE ROUND(SUM(o.net_revenue) / COUNT(DISTINCT o.order_id), 0) END as "AOV"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1
  ORDER BY 3 DESC

[KẾT QUẢ]
  Tổng rows : 2
  Columns   : Phân khúc, Đơn hàng, Doanh thu, AOV
  Phân khúc | Đơn hàng | Doanh thu | AOV
  VALUE_BRONZE | 3 | 1567711 | 522570
  VALUE_SILVER | 1 | 816296 | 816296

──────────────────────────────────────────────────────────────────────
Card 27/35: "Orders by Status"
card_id=1243 | dashcard_id=1736 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      o.status as "Trạng thái",
      COUNT(DISTINCT o.order_id) as "Đơn hàng"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
  GROUP BY 1
  ORDER BY 2 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      o.status as "Trạng thái",
      COUNT(DISTINCT o.order_id) as "Đơn hàng"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1
  ORDER BY 2 DESC

[KẾT QUẢ]
  Tổng rows : 3
  Columns   : Trạng thái, Đơn hàng
  Trạng thái | Đơn hàng
  OPEN | 3
  CANCELLED | 2
  DRAFT | 1

──────────────────────────────────────────────────────────────────────
Card 28/35: "Payment Method Distribution"
card_id=1244 | dashcard_id=1737 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      pm.payment_method_name as "PTTT",
      COUNT(*) as "Giao dịch",
      COALESCE(SUM(p.amount), 0) as "Số tiền"
  FROM fact_payments p
  JOIN dim_payment_methods pm ON p.payment_method_key = pm.payment_method_key
  JOIN fact_orders o ON p.order_id = o.order_id
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
  GROUP BY 1
  ORDER BY 3 DESC

[SQL THỰC TẾ (sau substitute)]
  SELECT
      pm.payment_method_name as "PTTT",
      COUNT(*) as "Giao dịch",
      COALESCE(SUM(p.amount), 0) as "Số tiền"
  FROM fact_payments p
  JOIN dim_payment_methods pm ON p.payment_method_key = pm.payment_method_key
  JOIN fact_orders o ON p.order_id = o.order_id
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND p.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)
  GROUP BY 1
  ORDER BY 3 DESC

[KẾT QUẢ]
  Tổng rows : 0
  Columns   : PTTT, Giao dịch, Số tiền
  ⚠️  0 rows — filter có thể quá hẹp hoặc thiếu data

──────────────────────────────────────────────────────────────────────
Card 29/35: "Discount Impact"
card_id=1245 | dashcard_id=1738 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT
      COUNT(DISTINCT o.order_id) as "Tổng đơn",
      COUNT(DISTINCT CASE WHEN o.discount_amount > 0 THEN o.order_id END) as "Đơn có CK",
      ROUND(COUNT(DISTINCT CASE WHEN o.discount_amount > 0 THEN o.order_id END) * 100.0
          / NULLIF(COUNT(DISTINCT o.order_id), 0), 1) as "Tỷ lệ CK %",
      SUM(COALESCE(o.discount_amount, 0)) as "Tổng CK",
      ROUND(AVG(CASE WHEN o.discount_amount > 0
          THEN o.discount_amount * 100.0 / NULLIF(o.gross_revenue, 0) END), 1) as "TB CK %"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT
      COUNT(DISTINCT o.order_id) as "Tổng đơn",
      COUNT(DISTINCT CASE WHEN o.discount_amount > 0 THEN o.order_id END) as "Đơn có CK",
      ROUND(COUNT(DISTINCT CASE WHEN o.discount_amount > 0 THEN o.order_id END) * 100.0
          / NULLIF(COUNT(DISTINCT o.order_id), 0), 1) as "Tỷ lệ CK %",
      SUM(COALESCE(o.discount_amount, 0)) as "Tổng CK",
      ROUND(AVG(CASE WHEN o.discount_amount > 0
          THEN o.discount_amount * 100.0 / NULLIF(o.gross_revenue, 0) END), 1) as "TB CK %"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Tổng đơn, Đơn có CK, Tỷ lệ CK %, Tổng CK, TB CK %
  Tổng đơn | Đơn có CK | Tỷ lệ CK % | Tổng CK | TB CK %
  4 | 3 | 75 | 2052500 | 52.1

──────────────────────────────────────────────────────────────────────
Card 30/35: "Chu kỳ báo cáo"
card_id=1437 | dashcard_id=2051 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE {{ordered_at}} AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[SQL THỰC TẾ (sau substitute)]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Chu kỳ báo cáo
  Chu kỳ báo cáo
  📅 Ngày báo cáo: 08/06/2026

──────────────────────────────────────────────────────────────────────
Card 31/35: "Chu kỳ báo cáo"
card_id=1941 | dashcard_id=2900 | tab="Kênh bán hàng"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE {{ordered_at}} AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[SQL THỰC TẾ (sau substitute)]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Chu kỳ báo cáo
  Chu kỳ báo cáo
  📅 Ngày báo cáo: 08/06/2026

──────────────────────────────────────────────────────────────────────
Card 32/35: "Chu kỳ báo cáo"
card_id=1942 | dashcard_id=2902 | tab="Sản phẩm"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE {{ordered_at}} AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[SQL THỰC TẾ (sau substitute)]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Chu kỳ báo cáo
  Chu kỳ báo cáo
  📅 Ngày báo cáo: 08/06/2026

──────────────────────────────────────────────────────────────────────
Card 33/35: "Chu kỳ báo cáo"
card_id=1943 | dashcard_id=2904 | tab="Khách hàng & Thanh toán"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE {{ordered_at}} AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[SQL THỰC TẾ (sau substitute)]
  SELECT '📅 Ngày báo cáo: ' ||
      strftime(
          (SELECT MIN(ordered_at)::DATE FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ? AND scope_retail),
          '%d/%m/%Y'
      ) AS "Chu kỳ báo cáo"

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Chu kỳ báo cáo
  Chu kỳ báo cáo
  📅 Ngày báo cáo: 08/06/2026

──────────────────────────────────────────────────────────────────────
Card 34/35: "Cancelled Orders"
card_id=822 | dashcard_id=3388 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COUNT(DISTINCT o.order_id) as "Cancelled Orders"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND NOT o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT COUNT(DISTINCT o.order_id) as "Cancelled Orders"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND NOT o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Cancelled Orders
  Cancelled Orders
  2

──────────────────────────────────────────────────────────────────────
Card 35/35: "Total Discounts"
card_id=2183 | dashcard_id=3389 | tab="Tổng quan"

[PARAMETER MAPPINGS]
  param slug="ordered_at" | target=["dimension",["template-tag","ordered_at"]] → value="thisday"

[SQL TEMPLATE]
  SELECT COALESCE(SUM(o.discount_amount), 0) as "Total Discounts"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE {{ordered_at}}
    AND o.scope_retail
    AND o.is_active_order

[SQL THỰC TẾ (sau substitute)]
  SELECT COALESCE(SUM(o.discount_amount), 0) as "Total Discounts"
  FROM fact_orders o
  JOIN dim_customers c ON o.customer_key = c.customer_key
  WHERE o.scope_retail
    AND o.is_active_order
    AND o.order_id IN (SELECT order_id FROM fact_orders WHERE "main"."fact_orders"."ordered_at" >= ? AND "main"."fact_orders"."ordered_at" < ?)

[KẾT QUẢ]
  Tổng rows : 1
  Columns   : Total Discounts
  Total Discounts
  2052500

══════════════════════════════════════════════════════════════════════
Debug hoàn tất.
══════════════════════════════════════════════════════════════════════
