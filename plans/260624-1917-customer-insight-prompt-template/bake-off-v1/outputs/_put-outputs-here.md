# Thả 15 output writer vào đây

Quy ước tên: `{writer}-{NN}.json` — writer ∈ {gpt, gemini, deepseek}, NN ∈ 01..05 (khớp số prompt).

Ví dụ: `gpt-01.json`, `gemini-03.json`, `deepseek-05.json`.

Xem chi tiết ở `../scorecard.md`. (File này chỉ để giữ thư mục, có thể xóa khi đã có output.)
