# Thả 31 output GPT vào đây

Quy ước: `script-{NN}-{customer_id}.json` — khớp với `../prompts/pilot-{NN}-{customer_id}.txt`.
Ví dụ: prompt `pilot-01-603264280.txt` → output `script-01-603264280.json`.

Chạy 31 prompt trên GPT theo thứ tự NN (ưu tiên cao trước). Xong → báo tôi spot-check.
