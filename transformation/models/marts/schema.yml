version: 2

models:
  # Dimensions
  - name: dim_customers
    description: "Customer dimension with CLV and RFM metrics."
    columns:
      - name: customer_key
        tests: [unique, not_null]
      - name: lifetime_value
        description: "Total collected from customer (after discounts, incl. tax). Formula: SUM(fact_orders.total_collected)"
      - name: recency_days
        description: "Days since last order. Formula: CURRENT_DATE - MAX(order_date)"
      - name: frequency
        description: "Total count of distinct orders. Formula: COUNT(order_id)"
      - name: customer_status
        description: "RFM Status. Logic: Active (<=30 days), At Risk (31-90 days), Churned (>90 days)"
      - name: value_group
        description: "Customer value tier: VALUE_VIP (>=50M or >=20 orders), VALUE_GOLD (>=20M), VALUE_SILVER (>=5M), VALUE_BRONZE (else)"
        data_type: varchar
        tests:
          - accepted_values:
              values: ['VALUE_VIP', 'VALUE_GOLD', 'VALUE_SILVER', 'VALUE_BRONZE']

  - name: dim_customers_base
    description: "Base customer dimension (SCD Type 1) without circular dependencies."
    columns:
      - name: customer_key
        tests: [unique, not_null]
      - name: dob
        description: "Date of Birth"
      - name: sex
        description: "Gender/Sex of the customer"
      - name: customer_group
        description: "Customer Group Code/Name"
      - name: loyalty_point
        description: "Loyalty points balance"


  - name: dim_products
    description: "Product dimension derived from sales."
    columns:
      - name: product_key
        tests: [unique, not_null]
      - name: brand_name
        description: "Standardized product brand name"
        data_type: varchar
      - name: brand_code
        description: "Product brand abbreviation code"
        data_type: varchar

  - name: dim_channels
    description: "Sales channel dimension."
    columns:
      - name: channel_key
        tests: [unique, not_null]
      - name: channel_name
        data_type: varchar
      - name: channel_code
        data_type: varchar
      - name: channel_category
        description: "Tier 1. Online-Ecommerce / Offline / Internal."
        data_type: varchar
        tests:
          - accepted_values:
              values: ['Online-Ecommerce', 'Offline', 'Internal']
      - name: channel_format
        description: "Tier 2. Marketplace / Social / Web / Retail / B2B / Direct / System / CrossBorder Fulfillment / Other."
        data_type: varchar
        tests:
          - accepted_values:
              values: ['Marketplace', 'Social', 'Web', 'Retail', 'B2B', 'Direct', 'System', 'CrossBorder Fulfillment', 'Other']
      - name: platform
        data_type: varchar
      - name: channel_brand
        description: "Brand that owns this channel"
        data_type: varchar
      - name: market
        description: "Domestic / Export"
        data_type: varchar
      - name: source_type
        description: "Source classification: channel, customer_type, team, purpose, arrangement"
        data_type: varchar
        tests:
          - accepted_values:
              values: ['channel', 'customer_type', 'team', 'purpose', 'arrangement']
      - name: is_sales_channel
        description: "true = real sales channel (not System/Internal)"
        data_type: boolean

  - name: dim_staff
    description: "Salesperson/Staff dimension."
    columns:
      - name: staff_key
        tests: [unique, not_null]

  - name: dim_time
    description: "Time dimension (Minute grain) with business hour flags."
    columns:
      - name: time_key
        description: "Integer HHMM (e.g., 0930, 2359)"
        tests: [unique, not_null]
      - name: time_of_day_24
        description: "HH:MM format"
      - name: time_of_day_12
        description: "HH:MM AM/PM format"
      - name: hour_24
        description: "Hour of day (0-23)"
      - name: is_business_hour
        description: "Boolean: Is between 09:00 and 17:00"
      - name: is_peak_hour
        description: "Boolean: Is Lunch (11-14) or Dinner (16-19)"
      - name: day_period
        description: "Morning, Afternoon, Evening, Night"

  - name: dim_date
    description: "Calendar date dimension."
    columns:
      - name: date_key
        description: "Integer date key (YYYYMMDD)"
        tests: [unique, not_null]
      - name: date_actual
        description: "Actual date value"

  - name: dim_branch_location
    description: "Location/Warehouse dimension."
    columns:
      - name: branch_location_key
        tests: [unique, not_null]

  - name: dim_payment_methods
    description: "Payment method dimension."
    columns:
      - name: payment_method_key
        tests: [unique, not_null]

  - name: dim_order_status
    description: "Order status dimension (Static)."
    columns:
      - name: status_key
        tests: [unique, not_null]

  - name: dim_product_types
    description: "Product type dimension."
    columns:
      - name: product_type_key
        tests: [unique, not_null]

  - name: dim_promotions
    description: "Promotion dimension."
    columns:
      - name: promotion_key
        tests: [unique, not_null]

  - name: dim_geography
    description: "Geography dimension."
    columns:
      - name: geography_key
        tests: [unique, not_null]

  # Facts
  - name: fact_sales
    description: "Transactional sales fact (line item grain)."
    columns:
      - name: order_id
        tests: [not_null]
      - name: product_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_products')
                field: product_key
      - name: product_type_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_product_types')
                field: product_type_key
      - name: seller_staff_key
        description: "Người chốt/được giao đơn (Sapo assignee). Primary for team attribution & commission."
        tests:
          - relationships:
              arguments:
                to: ref('dim_staff')
                field: staff_key
      - name: creator_staff_key
        description: "Người tạo đơn trên Sapo (Sapo account). Operational; use as fallback only."
        tests:
          - relationships:
              arguments:
                to: ref('dim_staff')
                field: staff_key
      - name: status_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_order_status')
                field: status_key
      - name: customer_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_customers_base')
                field: customer_key
      - name: branch_location_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_branch_location')
                field: branch_location_key
      - name: channel_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_channels')
                field: channel_key
      - name: date_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_date')
                field: date_key
      - name: shipping_geography_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_geography')
                field: geography_key
      - name: billing_geography_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_geography')
                field: geography_key
      - name: time_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_time')
                field: time_key
      - name: revenue
        description: "Total revenue (quantity * line_amount)"
      - name: discount_amount
        description: "Direct discount amount"
      - name: distributed_discount_amount
        description: "Distributed discount amount"

  - name: fact_orders
    description: "Aggregated order fact."
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: customer_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_customers_base')
                field: customer_key
      - name: shipping_geography_key
        data_type: varchar
        tests:
          - relationships:
              arguments:
                to: ref('dim_geography')
                field: geography_key
              config:
                severity: warn
      - name: billing_geography_key
        data_type: varchar
        tests:
          - relationships:
              arguments:
                to: ref('dim_geography')
                field: geography_key
              config:
                severity: warn
      - name: channel_key
        data_type: varchar
        tests:
          - relationships:
              arguments:
                to: ref('dim_channels')
                field: channel_key
      - name: promotion_key
        data_type: varchar
        tests:
          - relationships:
              arguments:
                to: ref('dim_promotions')
                field: promotion_key
              config:
                severity: warn
      - name: seller_staff_key
        data_type: varchar
        description: "Người chốt/được giao đơn (Sapo assignee). Primary for team attribution & commission."
        tests:
          - relationships:
              arguments:
                to: ref('dim_staff')
                field: staff_key
      - name: creator_staff_key
        data_type: varchar
        description: "Người tạo đơn trên Sapo (Sapo account). Operational; use as fallback only."
        tests:
          - relationships:
              arguments:
                to: ref('dim_staff')
                field: staff_key
      - name: status_key
        data_type: varchar
        tests:
          - relationships:
              arguments:
                to: ref('dim_order_status')
                field: status_key
      - name: branch_location_key
        data_type: varchar
        tests:
          - relationships:
              arguments:
                to: ref('dim_branch_location')
                field: branch_location_key
      - name: date_key
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('dim_date')
                field: date_key
      - name: time_key
        data_type: integer
        tests:
          - relationships:
              arguments:
                to: ref('dim_time')
                field: time_key
      - name: gross_revenue
        description: "Giá bán × SL (trước chiết khấu & thuế). Formula: net_revenue + discount_amount"
      - name: discount_amount
        description: "Tổng chiết khấu (coupon, promotion, combo)"
      - name: net_revenue
        description: "Doanh thu thuần (sau chiết khấu, trước thuế). Field gốc từ Sapo $.total"
      - name: tax_amount
        description: "Thuế VAT (8% hoặc 10%)"
      - name: total_collected
        description: "Tổng thu từ khách (sau chiết khấu, gồm thuế). Formula: net_revenue + tax_amount"
      - name: client_details
        description: "JSON details about client (IP, User Agent)"
      - name: discount_codes
        description: "JSON list of discount codes"

  - name: fact_payments
    description: "Transactional payment fact (one row per payment attempt/transaction)."
    columns:
      - name: payment_key
        tests: [unique, not_null]
      - name: order_id
        tests: [not_null]
      - name: payment_method_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_payment_methods')
                field: payment_method_key
      - name: amount
        description: "Transaction amount"
      - name: status
        description: "Payment status (paid, pending, etc)"
      - name: payment_timestamp
        description: "Time of payment creation"
      - name: paid_on
        description: "Time of payment completion"

  - name: fact_order_economics
    description: "Per-order profitability: Sapo revenue + MISA COGS + Shopee platform fees. Grain: one row per order."
    columns:
      - name: order_id
        tests: [unique, not_null]
      - name: order_code
        tests: [not_null]
      - name: channel_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_channels')
                field: channel_key
      - name: date_key
        tests:
          - relationships:
              arguments:
                to: ref('dim_date')
                field: date_key
      - name: gross_revenue
        description: "Giá bán × SL (trước chiết khấu & thuế)"
      - name: net_revenue
        description: "Doanh thu thuần (sau chiết khấu, trước thuế)"
      - name: cogs_amount
        description: "Giá vốn hàng bán (từ MISA). NULL nếu chưa có dữ liệu MISA."
      - name: has_cogs
        description: "true nếu đơn có dữ liệu COGS từ MISA"
      - name: gross_profit
        description: "Lãi gộp = net_revenue - COGS. Nếu không có COGS thì = net_revenue."
      - name: gross_margin_pct
        description: "Biên lãi gộp = gross_profit / net_revenue"
      - name: shopee_platform_fees
        description: "Tổng phí sàn Shopee (service + payment + fixed). NULL cho non-Shopee."
      - name: shopee_net_settlement
        description: "Số tiền Shopee thực chuyển về. NULL cho non-Shopee."
      - name: channel_net_profit
        description: "Lãi ròng kênh = net_revenue - COGS - phí sàn - thuế sàn"
      - name: channel_net_margin_pct
        description: "Biên lãi ròng kênh = channel_net_profit / net_revenue"
