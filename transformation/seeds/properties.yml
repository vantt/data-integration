version: 2

seeds:
  - name: ref_order_sources
    config:
      column_types:
        id: integer
        name: varchar
        status: boolean
        platform_group: varchar
        is_generic_source: boolean
        platform: varchar
        mapping_tag: varchar
