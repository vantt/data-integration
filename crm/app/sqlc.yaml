version: "2"
sql:
  - engine: "sqlite"
    schema: "../migrations"
    queries: "./internal/adapters/outbound/sqlite/queries"
    gen:
      go:
        package: "sqlcgen"
        out: "./internal/adapters/outbound/sqlite/sqlcgen"
        emit_json_tags: true
        emit_prepared_queries: false
