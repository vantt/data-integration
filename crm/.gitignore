# SQLite runtime files — never commit (anywhere under crm/, incl. data dirs created by `go run`)
*.db
*.db-wal
*.db-shm
**/data/
/bin/
*.exe

# templ-generated files — rebuilt via `templ generate`, never committed
*_templ.go
__pycache__/
.venv/
/sync/.venv/
*.duckdb
