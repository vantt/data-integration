# Pinned deliberately (was :latest — auto-upgrades can break dashboards).
# To upgrade deliberately: bump this tag, rebuild, verify Rill dashboards, then commit.
# Bumped 2026-07-07: v0.85.3 -> v0.87.8.
FROM rilldata/rill:v0.87.8

# Default project dir — mount your rill/ folder here
WORKDIR /app/rill

