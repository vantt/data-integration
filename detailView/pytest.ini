[pytest]
# Run from the detailView app root so `app` and `tests` import cleanly.
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    integration: live-DB smoke tests, skipped unless the real olap.duckdb is present
filterwarnings =
    ignore::DeprecationWarning
