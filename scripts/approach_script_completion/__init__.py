"""approach_script_completion — hexagon port cho generate_approach_scripts.py.

Scope hẹp: nhận prompt approach-script đã lắp sẵn, trả raw text completion.
KHÔNG phải LLM abstraction tổng quát cho repo — chỉ phục vụ đúng generator này.
"""
