---
name: setup-metabase-mcp
description: Set up and configure the Metabase MCP server for AI coding agents such as Claude Code, Antigravity, or Codex. Full documentation lives in .skills/setup-metabase-mcp/.
---

# Setup Metabase MCP

**Full instructions**: Read `.skills/setup-metabase-mcp/SKILL.md` before proceeding.
**Source of truth**: `.skills/setup-metabase-mcp/` owns all Setup Metabase MCP instructions.

Do not duplicate instructions here. Keep this wrapper thin so updates only need to happen in `.skills/setup-metabase-mcp/`.
