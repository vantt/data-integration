---
name: purge-dagster-runs
description: Clean up and purge old Dagster runs from the SQLite database running in the local Docker environment. Full documentation lives in .skills/purge-dagster-runs/.
---

# Purge Dagster Runs Skill

**Full instructions**: Read `.skills/purge-dagster-runs/SKILL.md` before proceeding.
**Source of truth**: `.skills/purge-dagster-runs/` owns all Purge Dagster Runs instructions.

Do not duplicate instructions here. Keep this wrapper thin so updates only need to happen in `.skills/purge-dagster-runs/`.
