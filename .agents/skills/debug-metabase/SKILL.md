---
name: debug-metabase
description: Explain or debug a Metabase card/dashboard — trace filters to actual SQL, rows, and business logic when a metric looks wrong or unexplained. Full documentation lives in .skills/debug-metabase/.
---

# Debug Metabase Skill

**Full instructions**: Read `.skills/debug-metabase/SKILL.md` before proceeding.
**Source of truth**: `.skills/debug-metabase/` owns all Debug Metabase instructions and scripts (`scripts/metabase-dashboard-debugger.js`).

Do not duplicate instructions here. Keep this wrapper thin so updates only need to happen in `.skills/debug-metabase/`.
